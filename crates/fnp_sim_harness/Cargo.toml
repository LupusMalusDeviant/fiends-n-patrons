[package]
name = "fnp_sim_harness"
description = "Headless bot runs, determinism checks and balancing simulations for Fiends n Patrons."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license-file.workspace = true
publish.workspace = true

[dependencies]
# The scenes name the game's own patterns, so the harness embeds the same units the game plays.
fnp_content.workspace = true
fnp_game.workspace = true
grimoire.workspace = true

# The golden-master narrowing (engine ADR-0018) compares one thread against a pool, and the
# determinism tests run the golden scenario with 1, 2 and N threads (engine ADR-0006, building
# block 7).
grimoire_exec.workspace = true

[lints]
workspace = true
